import SearchBar from "./SearchBar";

function Header() {
    return (
        <header>
            <h1>Fantasy Books</h1>
            <SearchBar />
        </header>
    );
}

export default Header;
